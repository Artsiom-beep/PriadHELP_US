import numpy as np
import pandas as pd

a = np.array([[1,2,3,4,5], [5,4,3,2,1]])

x = np.linspace(0, 10, 25)

# Как работает np.linspace:
# np.linspace(start, stop, num)
# создаёт равномерно распределённые числа на отрезке от start до stop (включительно).
#
# В твоём случае:
#
# start = 0
#
# stop = 10
#
# num = 25 → количество чисел

x = np.logspace(0, 10, 10, base=2)

# Общий вид
# np.logspace(start, stop, num, base)
#
# Что делает именно этот вызов:
#
# start = 0
#
# stop = 10
#
# num = 10 → количество чисел
#
# base = 2

# Функция возвращает 10 чисел, равномерно распределённых по показателю степени, а не по значению.
#
# То есть считается так:
#
# 2^0, 2^(10/9), 2^(20/9), ..., 2^10
#
# Фактически:
#
# Это эквивалентно:
#
# 2 ** np.linspace(0, 10, 10)
#
# Пример результата (примерно):
# [   1.            2.16          4.67
#     10.08        21.77         47.06
#    101.59       219.74        475.68
#   1024.        ]
#
# Главное отличие от linspace:
# linspace	logspace
# Линейный шаг	Экспоненциальный рост
# 0, 1, 2, 3	1, 2, 4, 8
# Для обычных осей	Для логарифмических шкал



x, y = np.mgrid[0:5, 0:5]
# print(x)
# print(y)

x = np.random.rand(5, 5)
#случайная матрица размером 5 на пять - от 1 до 0


x = np.random.randn(5, 5)
# liczby losowe z zakresu [0,1] - rozkład normalny: średnia = 0, odchylenie standardowe = 1

# x = np.random.randn(5, 5)
# np.random.randn генерирует числа из нормального распределения:
#
# среднее (μ) = 0
#
# стандартное отклонение (σ) = 1
#
# ⚠️ Комментарий в коде немного неверный:
# это НЕ диапазон [0,1], а нормальное распределение.
#
# Какой разброс значений?
# Для нормального распределения с σ = 1 действует правило:
#
# ~68% значений в диапазоне [-1, 1]
#
# ~95% значений в диапазоне [-2, 2]
#
# ~99.7% значений в диапазоне [-3, 3]
#
# 👉 Значения могут быть любыми (и отрицательные, и больше 3),
# но чаще всего ты увидишь числа примерно от -3 до 3.
#
# Пример возможного вывода
# Копировать код
# [[ 0.12 -1.03  0.56  1.87 -0.44]
#  [-0.77  0.34 -2.15  0.98  0.11]
#  [ 1.45 -0.22 -0.66  0.09 -1.91]
#  [ 0.33  2.04 -0.58 -1.12  0.67]
#  [-0.84  0.15  1.29 -0.47  0.02]]
# Если нужен диапазон [0, 1]
# Используй:
# np.random.rand(5, 5)
# Если нужен другой разброс
# np.random.normal(mean, std, size)
# Например:
# np.random.normal(10, 2, (5, 5))
# → среднее 10, разброс ±2.

x = np.diag([5, 5, 5])
# [[5 0 0]
#  [0 5 0]
#  [0 0 5]]


#print(A)
# wartość średnia
#np.mean(A)
# odchylenie standardowe i wariancja
#np.std(A[:,3]), np.var(A[:,3]), np.min(A[:,2]), np.max(A[:,2])
# wartości minimalne i maksymalne

# Разберём построчно, что именно считается и по какому принципу.
#
# Предположим, что A — это двумерная tablica NumPy (macierz).
#
# print(A)
#
#
# Просто выводит содержимое массива.
#
# 1️⃣ Średnia wartość
# np.mean(A)
#
#
# 👉 Считает średnią арифметyczną wszystkich элементов массива A
# (то есть по всем строкам и столбцам сразу).
#
# Если нужно:
#
# średnia po kolumnach → np.mean(A, axis=0)
#
# średnia po wierszach → np.mean(A, axis=1)
#
# 2️⃣ Odchylenie standardowe i wariancja
# np.std(A[:,3])
# np.var(A[:,3])
#
# A[:, 3] означает:
#
# : → все строки
#
# 3 → 4-я kolumna
#
# 👉 Берётся одна kolumna.
#
# Что считается:
#
# np.std(...) → odchylenie standardowe (σ)
#
# np.var(...) → wariancja (σ²)
#
#
#
# Wartości minimalne i maksymalne
# np.min(A[:,2])
# np.max(A[:,2])
#
#
# A[:,2] → 3-я kolumna
#
# np.min → najmniejsza wartość
#
# np.max → największa wartość
#
# 🔹 Podsumowanie
# Funkcja	Co robi
# np.mean(A)	Średnia całej macierzy
# np.std(A[:,3])	Odchylenie standardowe kolumny
# np.var(A[:,3])	Wariancja kolumny
# np.min(A[:,2])	Minimum kolumny
# np.max(A[:,2])	Maksimum kolumny